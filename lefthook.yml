pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: yarn eslint --config eslint.full.config.mjs --max-warnings=0 --no-warn-ignored {staged_files}
    typecheck:
      glob: "*.{js,ts,jsx,tsx}"
      run: yarn tsc --noEmit
